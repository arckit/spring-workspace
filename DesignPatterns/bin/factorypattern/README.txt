Code to interface to avoid the implementation is hidden fromthe consume
also the interface coding provides exposure to multiple implementations and flexibility.
 if one implememntation is not good then the only thing that changes in the impl code and not the calling code.